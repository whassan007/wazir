{
  "name": "@wazir/api",
  "version": "0.1.28",
  "description": "HTTP API server for the meta-harness",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@wazir/agents": "*",
    "@wazir/core": "*",
    "@wazir/models": "*",
    "@wazir/policies": "*",
    "@wazir/runtimes-lmstudio": "*",
    "@wazir/runtimes-ollama": "*",
    "@wazir/scheduler": "*",
    "@wazir/shared": "*",
    "@wazir/tools": "*",
    "@wazir/workers": "*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "ts-node-dev": "^2.0.0"
  }
}

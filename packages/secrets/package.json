{
  "name": "@wazir/secrets",
  "version": "0.1.28",
  "description": "Secret Broker: durable credential storage (OS keychain, with an encrypted-file fallback) for the meta-harness",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@wazir/shared": "*"
  },
  "optionalDependencies": {
    "@napi-rs/keyring": "^2.1.0"
  }
}

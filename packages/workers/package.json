{
  "name": "@wazir/workers",
  "version": "0.1.0",
  "description": "Wazir worker: executes authorized requests on a target computer",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wazir/core": "*",
    "@wazir/runtimes-interfaces": "*",
    "@wazir/runtimes-ollama": "*",
    "@wazir/runtimes-lmstudio": "*",
    "@wazir/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}

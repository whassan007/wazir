{
  "name": "@wazir/agents",
  "version": "0.1.0",
  "description": "Agent adapters for the Wazir meta-harness (native coding agent + external agent providers)",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wazir/core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
